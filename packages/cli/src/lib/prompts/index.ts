/**
 * Re-export prompt utilities from the rover-prompts package.
 *
 * The canonical implementation lives in rover-prompts.
 * This module re-exports everything for existing CLI imports.
 */
export { PromptBuilder, type IPromptTask } from 'rover-prompts';
