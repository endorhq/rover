{
  "name": "rover-workspace",
  "version": "1.0.0",
  "author": "The Endor Team <hello@endor.dev>",
  "engines": {
    "pnpm": ">=10",
    "node": ">=22"
  },
  "packageManager": "pnpm@10.25.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter './packages/*' -r --parallel run dev",
    "build": "pnpm -r --filter './packages/*' build",
    "build-dev": "pnpm -r --filter './packages/*' build-dev",
    "package": "pnpm --filter './packages/*' package",
    "check": "pnpm --filter './packages/*' check",
    "e2e-test": "pnpm --filter './packages/*' e2e-test",
    "test": "pnpm --filter './packages/*' test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
